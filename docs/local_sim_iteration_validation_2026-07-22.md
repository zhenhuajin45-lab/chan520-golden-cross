# 本地模拟盘熊市小仓迭代与回溯验证（2026-07-22）

本文记录 2026-07-22 “核心计划为空、盘中无交易”问题的修复边界与验证结果。所有成交回溯均使用临时 SQLite 账本，未补记当日生产模拟盘成交。

## 根因与决策

- 08:22 扫描成功 4,976/4,994 只，覆盖率 99.64%；数据与调度并非当日无交易的直接原因。
- 市场状态为 `BEAR`，严格候选仅 1 只且未通过完整入场几何，核心账户可执行计划为 0；原有 23 只观察候选不会自动成交。
- 核心账户继续 fail-closed，不因单日反事实盈利放宽熊市禁买。
- 新增独立账户 `local-sim-bear-pilot`，仅承接完整价格证据的熊市防御研究候选：单票 2.5%、最多 2 只、总仓 5%、`R:R >= 2`。

## 已完成修改

- 核心计划生成器自动把候选理由、止损、目标、风险收益比、T+1 缓冲和仓位预算写入独立研究账户计划。
- 研究账户复用二阶段行情触发、报价新鲜度、停牌/涨跌停、市场冲击、同日风控回补禁买和 T+1 风险退出。
- 执行器双向校验账户与策略 ID；研究计划不能进入核心账户，研究账户也不能执行核心策略计划。
- 每日编排器在盘前、盘中、盘后分别运行研究账户导出、风险扫描、风险退出和买入触发，并使用独立审计文件。
- 工作台嵌入研究账户的资产、盈亏、仓位、计划、持仓、成交和理由；飞书计划、成交、复盘消息区分两个账户。
- 增加执行漏斗：股票池、成功扫描、严格候选、观察候选、核心可执行和熊市研究小仓。

## 真实快照回溯

输入为 `reports/scan_2026-07-21/market_scan_2026-07-21.csv` 与当日已记录的 `BEAR` 市场状态。

| 账户 | 计划结果 | 预算 |
|---|---|---:|
| 核心账户 | 0 只可执行 | 0% |
| 熊市研究小仓 | 天目药业 600671，1,200 股，止损 19.28，目标 22.75，R:R 2.0945 | 2.448% |
| 熊市研究小仓 | 国泰海通 601211，1,300 股，止损 18.26，目标 20.50，R:R 3.0102 | 2.447% |

两条计划合计预算 4.8946%。临时账本回放中，10:02 两只均只进入 `TRIGGER_CONFIRMED`，10:05 第二次确认后才成交；核心账户成交数保持 0。计划生成耗时 2.28 秒，明显低于 5 分钟 SLA。

## 验证结果

- 定向测试：39 项通过。
- 全量测试：204 项通过，耗时 4.58 秒。
- Python `py_compile` 与 JavaScript `node --check` 通过。
- 飞书 `plan/trades/review` 全模式 dry-run 通过，webhook 已配置，本次未发送。
- 工作台桌面 1440px 与移动 390px 检查通过，无页面横向溢出，15 秒自动刷新正常。
- launchd 的 plan/preopen/intraday/eod 与 8768 工作台任务仍指向当前项目目录，无需改写 plist。

## 继续保持关闭的边界

- 2026-07-22 盘中不追溯创建或成交研究计划；新规则从下一次盘前计划开始生效。
- 研究小仓仅属于本地模拟研究，不连接 GM，不影响核心账户。
- 正式 V5/GM open-close kernel 仍未完成回放、对账、断网、重复提交和部分成交演练，`auto_open_close_kernel_ready=false`、`gm_adapter_shadow_ready=false`、`shadow_readiness=false` 保持不变。
