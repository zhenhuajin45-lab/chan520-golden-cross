# 本地模拟盘五日复盘迭代验收（2026-08-02）

## 迭代边界

- 核心账户在 `BEAR` 中继续禁止新增买入，未放宽核心 open kernel。
- 熊市样本只进入独立账户 `local-sim-bear-pilot`：候选队列最多 10 只、单票 1%、每日最多成交 2 只、最多同时持仓 5 只、总仓 5%。
- `auto_open_close_kernel_ready=false`、`gm_adapter_shadow_ready=false`、`shadow_readiness=false` 保持不变。

## 修复项

1. 历史回放优先使用计划中显式 `BEAR_DEFENSIVE_WATCH` 子集，仅在无标签的旧报告中才启用 `legacy_shape_fallback`，消除候选集污染。
2. 将熊市研究策略升级为 `local_sim_bear_probe_v3`：优先 v1 `R:R >= 2` 候选，队列不足时才以 `max(1.5 ATR, 1.25R)` 结构目标补位，并保留 v1/v2 对照审计和旧 v2 计划兼容读取。
3. 新增连续五个交易日有候选但核心/probe 均为空的 `SIGNAL_STARVATION` 告警与恢复状态。
4. 同日重生成计划时，未再入选的活动计划标记为 `SUPERSEDED_PLAN_REFRESH`，避免重复触发。
5. 新增周五五日审计，工作台和飞书同时展示实际账本、当日盯市、T+1 下一交易日收盘和数据完整性。
6. 候选资格与成交容量解耦：所有合格候选按优先级入队，前排未触发不再阻塞后排；执行器仍独立限制每日 2 笔、最多 5 个持仓和 5% 总暴露。
7. 新增五日研究活跃度检查，目标至少 3 个触发日；该指标只用于诊断信号饥饿，明确禁止为达标强制交易。

## 7 月 27 日至 31 日回溯

| 交易日 | 计划候选 | 精确熊市子集 | 触发 | 当日收盘净盯市 |
|---|---:|---:|---:|---:|
| 2026-07-27 | 14 | 1 | 0 | 0.00 |
| 2026-07-28 | 22 | 5 | 0 | 0.00 |
| 2026-07-29 | 21 | 5 | 2 | -139.00 |
| 2026-07-30 | 21 | 2 | 2 | +268.00 |
| 2026-07-31 | 22 | 6 | 2 | -83.00 |

- 实际账本订单/成交：`0/0`；自动化每日计划、风控、触发和 EOD 证据完整。
- 精确研究子集在 5 个交易日中触发 3 天，共 6 笔独立研究成交；当日收盘净盯市合计 `+46.00` 元，不是可实现收益。
- 4 笔已有下一交易日数据的交易，T+1 收盘净观察为 `-182.65` 元；7 月 31 日的 2 笔按期末未实现保留。
- T+1 数据源为本地 SQLite 优先，腾讯精确日期补取并回写，本次缺失数为 0。
- 7 月 27 日未触发的主因是 `ABOVE_TRIGGER_ZONE`，不追高；7 月 28 日未触发的主因是 `BROAD_MARKET_SHOCK_BLOCKED`，不绕过市场冲击门。

## 8 月 3 日计划验证

- 信号日 `2026-07-31`，市场状态 `BEAR`，核心可执行计划 0。
- 全市场 4,997 只中成功扫描 4,977 只；63 只进入观察池，10 只熊市防御候选全部进入独立 probe 队列。
- 队列前两只是 `002668 TCL智家` 900 股、`002303 美盈森` 2,100 股；其余候选不会因为前两只未触发而失去盘中检查机会。
- 10 只队列名义金额合计约 9.38%，这是互斥触发预算而非可同时成交仓位；每日成交上限 2 只、最多持仓 5 只、可执行总暴露仍严格为 5%。
- 周末 dry-run 识别 10 张有效计划并因不在连续竞价时段全部返回 `NOT_IN_CONTINUOUS_AUCTION`，成交数 0，fail-closed 符合预期。
