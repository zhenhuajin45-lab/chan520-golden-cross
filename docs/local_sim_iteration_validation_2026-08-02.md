# 本地模拟盘五日复盘迭代验收（2026-08-02）

## 迭代边界

- 核心账户在 `BEAR` 中继续禁止新增买入，未放宽核心 open kernel。
- 熊市样本只进入独立账户 `local-sim-bear-pilot`，单票 2.5%、最多 2 只、总仓 5%。
- `auto_open_close_kernel_ready=false`、`gm_adapter_shadow_ready=false`、`shadow_readiness=false` 保持不变。

## 修复项

1. 历史回放优先使用计划中显式 `BEAR_DEFENSIVE_WATCH` 子集，仅在无标签的旧报告中才启用 `legacy_shape_fallback`，消除候选集污染。
2. 新增 `local_sim_bear_probe_v2`：优先 v1 `R:R >= 2` 候选，名额不足时才以 `max(1.5 ATR, 1.25R)` 结构目标补位，并保留 v1/v2 对照审计。
3. 新增连续五个交易日有候选但核心/probe 均为空的 `SIGNAL_STARVATION` 告警与恢复状态。
4. 同日重生成计划时，未再入选的活动计划标记为 `SUPERSEDED_PLAN_REFRESH`，避免重复触发。
5. 新增周五五日审计，工作台和飞书同时展示实际账本、当日盯市、T+1 下一交易日收盘和数据完整性。

## 7 月 27 日至 31 日回溯

| 交易日 | 计划候选 | 精确熊市子集 | 触发 | 当日收盘净盯市 |
|---|---:|---:|---:|---:|
| 2026-07-27 | 14 | 1 | 0 | 0.00 |
| 2026-07-28 | 22 | 5 | 0 | 0.00 |
| 2026-07-29 | 21 | 5 | 2 | -339.63 |
| 2026-07-30 | 21 | 2 | 2 | +708.82 |
| 2026-07-31 | 22 | 6 | 2 | -223.40 |

- 实际账本订单/成交：`0/0`；自动化每日计划、风控、触发和 EOD 证据完整。
- 精确研究子集当日收盘净盯市合计 `+145.79` 元，不是可实现收益。
- 4 笔可观察交易的 T+1 下一交易日收盘净观察为 `-406.72` 元；7 月 31 日的 2 笔按期末未实现保留。
- T+1 数据源为本地 SQLite 优先，腾讯精确日期补取并回写，本次缺失数为 0。

## 8 月 3 日计划验证

- 信号日 `2026-07-31`，市场状态 `BEAR`，核心可执行计划 0。
- 独立 probe 选中 `002668 TCL智家` 2,300 股和 `002303 美盈森` 5,400 股，预计总暴露约 4.91%，两者均为 v1 `R:R >= 2` 优先候选。
- 重算前候选 `600919/003013` 已标记 `SUPERSEDED_PLAN_REFRESH`，不再进入触发器。
- 周末 dry-run 识别 2 张有效计划并因未到交易日而 `MARKET_CONTEXT_UNAVAILABLE`，成交数 0，fail-closed 符合预期。
